package com.studygenai.ui.components

import androidx.compose.runtime.Composable
import com.studygenai.domain.model.Note

// TODO: Reusable card showing note title, subject, date, image thumbnail
// Used in: LibraryScreen, DashboardScreen (recent notes list)
@Composable
fun NoteCard(note: Note, onClick: () -> Unit, onDelete: () -> Unit) {
    // Implementation goes here
}
