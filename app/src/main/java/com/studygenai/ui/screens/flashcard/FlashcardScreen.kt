package com.studygenai.ui.screens.flashcard

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

// TODO: Receives noteId as nav argument
// HorizontalPager of flippable cards (front = term, back = definition)
// Bottom bar: card counter + Flip button
// Generates cards on first visit, cached in Firestore after
@Composable
fun FlashcardScreen(navController: NavController, noteId: String) {
    // Implementation goes here
}
