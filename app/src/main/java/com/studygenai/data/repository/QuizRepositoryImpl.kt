package com.studygenai.data.repository

// TODO: Implements QuizRepository (domain)
// Coordinates: FirestoreService (get note text) → GeminiClient → FirestoreService (save quiz)
class QuizRepositoryImpl
