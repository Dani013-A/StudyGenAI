package com.studygenai.data.repository

// TODO: Implements FlashcardRepository (domain)
// Coordinates: FirestoreService (get note text) → GeminiClient → FirestoreService (save cards)
class FlashcardRepositoryImpl
