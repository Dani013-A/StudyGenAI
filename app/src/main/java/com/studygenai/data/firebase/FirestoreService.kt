package com.studygenai.data.firebase

// TODO: All Firestore reads and writes
// Collections: users/{uid}/notes, /quizzes, /flashcards
// Returns Kotlin Flow via snapshot listeners
class FirestoreService
